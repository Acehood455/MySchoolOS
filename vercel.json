{
  "framework": "vite",
  "installCommand": "npm ci --workspaces --include-workspace-root --include=dev",
  "buildCommand": "npm ls react tailwindcss @tanstack/react-query react-router-dom zod && npm run build --workspace @myschoolos/web",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
